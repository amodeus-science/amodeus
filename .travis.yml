before_install:
  - sudo apt-get -qq update > /dev/null
  - sudo apt-get install swig > /dev/null
  - ./travis_script.sh > /dev/null
  - export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/travis/glpk/lib/jni

language: java
jdk:
  - oraclejdk8

notifications:
  email: false

